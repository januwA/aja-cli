import "./main.styl";

import { bootstrapModule } from "@aja";
import { AppModule } from "./app/app.module";

// try {
  bootstrapModule(AppModule);
// } catch (error) {
  // console.error(error);
// }
